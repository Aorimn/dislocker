language: c

compiler:
    - clang
    - gcc

os:
    - linux
    - osx

install:
    - make travis-install

before_script:
    - cd src

script:
    - make CC=$CC
    - sudo make install
    - dislocker --help

notifications:
    email:
        recipients:
            - aorimn@gmail.com

        on_success: always
        on_failure: always
