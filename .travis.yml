language: c

compiler:
    - clang
    - gcc

install:
    - sudo apt-get update -qq
    - sudo apt-get install -qq libfuse-dev libpolarssl-dev

script: cd src && make

after_script: LD_LIBRARY_PATH=`pwd`/src/ ./src/dislocker --help

notifications:
    email:
        recipients:
            - aorimn@gmail.com

        on_success: always
        on_failure: always
